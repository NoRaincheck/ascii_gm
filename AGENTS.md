# AGENTS.md

## Display convention

Always only show the latest / single card in `docs/`. The GitHub Pages app must render exactly one card (the most recently generated one) and never accumulate or display a history of multiple cards.
